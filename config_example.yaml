app:
  name: "musannif"
  sqlite_directory: "./data"
  log_directory: "./logs"
  note_directory: "./notes"
  environment: "debug"
server:
  host: "localhost"
  port: 8080
secrets:
  JWT_ACCESS_SECRET: "your-access-secret"
  JWT_REFRESH_SECRET: "your-refresh-secret"

rag:
  service_url: "http://localhost:8000"
